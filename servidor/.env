# Configuración de conexión a la base de datos PostgreSQL
# Estos valores deben coincidir con los definidos en docker-compose.yml

DB_HOST=localhost
DB_PORT=5432
DB_NAME=altrix_db
DB_USER=altrix_user
DB_PASSWORD=altrix_pass

FLASK_ENV=development
